# Discord Bot Token
DISCORD_TOKEN=

# Discord Application Client ID (required for deploying commands)
CLIENT_ID=

# MongoDB Connection URI
MONGOBD_URI=

# Bot Command Prefix
PREFIX=!

# Environment (development/production)
NODE_ENV=development

# Optional: Guild ID for guild-specific command deployment (for instant updates during development)
# GUILD_ID=

# Note: Channel configurations (welcome, goodbye, log) are now set via /setbot dashboard
# No need to configure them here anymore
